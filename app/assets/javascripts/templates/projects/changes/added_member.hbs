<li class="institution_member change-item" data-person-id="{{person.id}}">
    Add <strong>{{person.name}}</strong>
    <a href="#" class="undo-action">Undo</a>

    <input type="hidden" value="{{membershipData}}" name="people_and_institutions_to_add[]"/>
</li>
