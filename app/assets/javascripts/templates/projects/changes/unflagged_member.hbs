<li class="institution_member change-item" data-person-id="{{person.id}}">
    Unflag <strong>{{person.name}}</strong> as leaving/left the project
    {{> projects/_undo_button}}

    <input type="hidden" value="1" name="memberships_to_unflag[{{id}}]"/>
</li>
