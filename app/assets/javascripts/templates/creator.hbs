<div>
    {{creator.name}} ({{creator.projects}})
    <a title="Remove creator" class="remove-association delete clickable" data-object-id="{{creator.id}}">&times;</a>
    <input type="hidden" value="{{creator.id}}" name="{{prefix}}[creator_ids][]"/>
</div>
