<% if standalone %>
    The total size of all project assets is:
<% end %>
<% cache({controller: 'projects', action: 'storage_report', id: project.id}, expires_in: 1.hour) do -%>
    <strong><%= number_to_human_size project.total_asset_size %></strong>
<% end %>
