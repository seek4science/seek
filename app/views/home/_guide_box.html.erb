
<% if !logged_in? || current_user.try(:show_guide_box?) %>
    <div id='guide_box' class='top_home_panel' >
      <% if logged_in? -%>
        <span id="guide_box_close">
          <%= link_to_remote(image('destroy',:method => :post, :title => tooltip_title_attrib("You may hide this box, and it will then no longer show up to you whilst logged into SEEK")), {:url => hide_guide_box_users_path()})%>
        </span>
      <% end -%>
      <p>
        To find out more information about using <%= Seek::Config.application_name -%>, some of our Guidelines and our Frequently Asked Questions please visit our <%= link_to 'Help and FAQ', help_documents_path %> section.
      </p>
      <p>
        We also distribute our JERM Templates for use with <%= Seek::Config.application_name -%>, which can be found in our <%= link_to 'Templates', help_documents_path + '/templates' %> section.
      </p>
    </div>
<% end %>