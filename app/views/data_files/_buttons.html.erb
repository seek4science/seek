<% if item.can_download? %>

  <%= button_link_to("Analyse", 'galaxy_button',
                     galaxy_analyse_setup_data_file_path(item, :code => params[:code]),
                     :title => "Analyse with Galaxy",
                     icon_options:{height:'18px'}) %>

<% end %>

<%= render :partial => 'assets/asset_buttons', :locals => {:asset => item, :version => version} -%>

