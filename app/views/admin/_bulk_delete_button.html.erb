<% path = polymorphic_path([:bulk_destroy, collection.first.class]) %>
<%= form_tag path do %>
    <% collection.each do |item| %>
        <%= hidden_field_tag "ids[]", item.id %>
    <% end %>
    <%= button_link_to('Destroy All', 'destroy', '#', confirm: 'Are you sure?', id: 'bulk') %>
<% end %>
