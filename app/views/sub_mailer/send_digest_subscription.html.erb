Dear <%= @person.name%>,
<br/>
<br/>
<% if @frequency == 'immediately' %>
The following resources have just been created/updated in <%= Seek::Config.project_name %> Seek:
<% else %>
The following resources have been created/updated in <%= Seek::Config.project_name %> Seek since last <%= @frequency%>:
<% end %>
<br/>
<br/>

<% @activity_logs.group_by(&:action).each do |action, logs| %>
    <label style="color:#f0342a;"><b><%= action.capitalize%></b></label>
    <div id="new_resources" class="new_resources" >
       <table border="1px" cellpadding="5" style="border-collapse: collapse;
              font-family: 'Lucida Grande', Tahoma, Arial, Helvetica, sans-serif;
              width: 100%;">

      <tr>
        <th align="left">Project Name</th>
        <th align="left">Resource Type</th>
        <th align="left">Resource Name</th>
        <th align="left">Person</th>
        <th align="left"><%= action.capitalize%> Date</th>
        <th align="left">Unsubscribe?</th>
      </tr>
      <% logs.each do |log| %>

              <tr>
                <td><%= log.referenced.nil? ? "<span style='font-style:italic;color:#666666'>Not Specified</span>" : log.referenced.name %></td>
                <td><%= log.activity_loggable_type.humanize %></td>
                <td><%= try_block{link_to(log.activity_loggable.title, send("#{log.activity_loggable.class.name.underscore}_url", log.activity_loggable))} || log.activity_loggable.title%></td>
                <td><%= link_to h(try_block{log.culprit.person.name}), "mailto:#{try_block{log.culprit.person.email_with_name}}" %></td>
                <td> <%= log.created_at %></td>
                <td> <%= link_to "Unsubscribe", Seek::Config.site_base_host.end_with?('/') ? "#{Seek::Config.site_base_host}" : "#{Seek::Config.site_base_host}/" + "subscriptions/destroy/#{Subscription.find_by_subscribable_id_and_subscribable_type_and_person_id(log.activity_loggable_id, log.activity_loggable_type, @person.id).id}" %></td>
              </tr>

      <% end %>
           </table></div>
    <br style="clear:both"/>
<% end %>
<br/>
You can change your subscription setting by editing your profile:
<%= link_to 'Edit profile', edit_person_path(@person) -%>.
<br/>

You can unsubscribe a particular resource by clicking the Unsubscribe link from the subscription report above
<br/>
or by visiting that resource in <%= Seek::Config.project_name %> Seek
<br/>
<br/>

kind regards,
<br/>
- The <%= Seek::Config.dm_project_name %> Team