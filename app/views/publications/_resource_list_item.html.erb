<%= list_item_title resource %>

<%= list_item_attribute "PubMed ID", resource.pubmed_id, "http://www.ncbi.nlm.nih.gov/pubmed/#{resource.pubmed_id}", {:popup => true}%>

<p class="list_item_attribute">
  <b>Author<%= resource.asset.creators.size + resource.non_seek_authors.size > 1 ? "s" : "" -%>:</b>
  <% if resource.asset.creators.empty? && resource.non_seek_authors.empty? %>
    <span class='none_text'>Not specified</span>
  <% else %>
    <% resource.asset.creators.each do |author| %>
      <%= (link_to get_object_title(author), show_resource_path(author)) + ((author == resource.asset.creators.last) && (resource.non_seek_authors.empty?) ? "" : ", ") %>
    <% end %>
    <% resource.non_seek_authors.each do |author| %>
      <%= author.last_name + " " + author.first_name + ((author == resource.non_seek_authors.last) ? "" : ", ") %>
    <% end %>
  <% end %>
</p>

<%= list_item_optional_attribute "Project", h(resource.project.try(:title)), resource.project %>

<%= list_item_expandable_text "Abstract", h(resource.abstract) %>

<%= list_item_optional_attribute "Journal", h(resource.journal) %>

<%= list_item_attribute "Date Published", resource.published_date.strftime('%d/%m/%Y') %>     
