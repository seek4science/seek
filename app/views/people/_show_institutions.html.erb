<% institutions=show_institutions %>

<div id="institutions">
  <% unless is_nil_or_empty? institutions %>
    <ul><label>Institutions:</label>
      <% for institution in institutions %>
        <li>
          <%= link_to h(institution.name),url_for(institution) %>
        </li>
      <% end  %>
    </ul>
  <% end  %>
</div>