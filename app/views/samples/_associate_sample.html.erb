<% if Seek::Config.biosamples_enabled %>
    <%#= fields_for(resource) {|f| f.fancy_multiselect :samples, {:hidden=> resource.samples.blank?  ? true : false, :other_projects_checkbox => true}}  -%>
<% end %>
