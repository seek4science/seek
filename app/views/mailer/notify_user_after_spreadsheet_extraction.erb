Hi <%= @user.person.name %>,

<% if @errors.empty? %>
  Your spreadsheet extraction for <%= @item_type.humanize %> with ID '<%= @item_id %>' has been successfully completed.
  Here are the results:
  <% @results.each do |res| %>
    - <%= res %>

  <% end %>
<% else %>
  Your spreadsheet extraction for <%= @item_type.humanize %> with ID '<%= @item_id %>' encountered one or more errors. Please correct them and try again.
  <% @errors.each do |err| %>
    - <%= err %>
    
  <% end %>
<% end %>
