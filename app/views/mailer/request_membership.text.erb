<% controller=@resource.class.name.underscore.pluralize -%>
Hi <%= @owners.collect{|o| o.name}.join(", ") -%>,

The <%= Seek::Config.application_name %> user <%= @requester.name -%>, <%= person_url(@requester) -%>, has requested membership of "<%= @resource.title -%>": <%= url_for(:action=>:show,:controller=>controller,:id=>@resource, :only_path => false) -%>.

<% unless @details.blank? -%>
    <%=@requester.first_name.capitalize -%> also provided some additional details:

    "<%= sanitize(@details) -%>"

<% end -%>
You can reply to <%= @requester.first_name.capitalize -%> by replying to this email.

This is an automated email.

- <%= Seek::Config.dm_project_name %> Team