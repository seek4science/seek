Hi <%= @name %>,

You have just joined a <%= t('project') %>.

An administrator, or <%= t('project') %> administrator, has assigned you to the following <%= t('project').pluralize(@projects.size) %>:
<% @projects.each do |project| %>
    * <%= link_to(project.title,project) -%>
<% end %>

Regards,

- The <%= Seek::Config.dm_project_name %> Team
