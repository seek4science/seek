Hi <%= @gatekeeper.name -%>,

    The <%= Seek::Config.application_name %> user <%= @requester.name -%>, <%= person_url(@requester,:host=>@host) -%>, has requested your approval to publish some items.
    You can approve or reject by clicking on the links beside the items:
    <% @resources.each do |resource|%>
        <% controller=resource.class.name.underscore.pluralize -%>
        "<%= resource.title -%>": <%= url_for(:controller => controller, :action => 'approve_or_reject_publish', :id => resource.id, :host=>@host) -%>

    <% end %>

    You can also reply to <%= @requester.first_name.capitalize -%> by replying to this email.

    This is an automated email.

- <%= Seek::Config.dm_project_name %> Team