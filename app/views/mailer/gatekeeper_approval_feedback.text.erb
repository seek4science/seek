Hi <%= @requester.name -%>,

    Upon your publishing requests, the <%= Seek::Config.application_name %> <%= t('asset_gatekeeper').downcase -%> <%= @gatekeeper.name -%>, <%= person_url(@gatekeeper) -%> approved to publish:
    <% @items_and_comments.each do |item_and_comment| %>
        <% item = item_and_comment[:item] %>
        <% controller=item.class.name.underscore.pluralize -%>
        <%= item.class.name.humanize -%>: <%= item.title -%> - <%= url_for(:action=>:show,:controller=>controller,:id=>item, :only_path => false) -%>

        Comment: <%= item_and_comment[:comment].blank? ? 'No comment' : item_and_comment[:comment] %>

    <% end %>
    The item(s) are now accessible in <%= Seek::Config.application_name %> without logging in.

    This is an automated email.

- <%= Seek::Config.dm_project_name %> Team.