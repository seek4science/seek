Hi,

The request to join a <%= t('project') %> has been responded to and accepted by <%= @responder.name %>.

The <%= t('project') %> joined is '<%= @project.title %>' ( <%= project_url(@project) %> ), and has been joined by
'<%= @requester.name %>' ( <%= person_url(@requester) %> ).

This is an automated email.