<%
  @project =  @assay.projects.first
%>

<% if Seek::Config.project_single_page_enabled && displaying_single_page? %>
  <%= render partial: 'general/single_page_treeview', locals: { item: @assay,
                                                                content_partial: 'assays/show_main_content',
                                                                item_type: 'assay', item_id: @assay.id,
                                                                project: @assay.projects.first} %>
<% else %>
  <%= render partial: 'show_main_content' %>
<% end %>



