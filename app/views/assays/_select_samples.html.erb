<%=
 render :partial => 'assets/fancy_multiselect', :layout => "assets/folding_box",
        :locals => {
                :fold_id => 'add_samples_form',
                :fold_title => (help_icon("Here you can associate the assay with specific samples.") + 'Samples'),
                :intro => "The following samples are involved in this #{@assay_text}:",
                :button_text => "Include in the #{@assay_text}",
                :default_choice_text => "Select Sample ...",
                :name => :assay,
                :method => :sample_ids,
                :possibilities => Sample.all.select(&:can_view?),
                :selected => @assay.samples.map(&:id)}
%>