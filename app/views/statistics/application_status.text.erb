<%
   search = Seek::Config.solr_enabled ? "enabled" : "disabled"
%>
<%= Seek::Config.instance_name %> is running | search is <%= search %> | <%= Seek::Util.live_job_workers.count %> background job worker processes running