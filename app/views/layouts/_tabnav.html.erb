<% if logged_in? && !current_user.person.nil? -%>
  <div>
    <ul class="tabnav">

      <!-- People tab -->
      <li <%= (["people"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %>>
        <%= link_to "People", people_url -%>
      </li>

      <!-- Projects tab -->
      <li <%= (["projects"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %>>
        <%= link_to "Projects", projects_url -%>
      </li>

      <!-- Institutions tab -->
      <li <%= (["institutions"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %>>
        <%= link_to "Institutions", institutions_url -%>
      </li>


      <!-- Investigations tab -->
      <li <%= (["investigations"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %> class="tab_gap_before">
        <%= link_to "Investigations", investigations_url -%>
      </li>

      <!-- Studies tab -->
      <li <%= (["studies"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %> >
        <%= link_to "Studies", studies_url -%>
      </li>

      <!-- Assays tab -->
      <li <%= (["assays"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %> >
        <%= link_to "Assays", assays_url -%>
      </li>

      <!-- Data tab -->
      <li <%= (["data_files"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %> class="tab_gap_before" >
        <%= link_to "Data", data_files_url -%>
      </li>

      <!-- Models tab -->
      <li <%= (["models"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"': '') %>
      <%= link_to "Models", models_url -%>
    </li>

    <!-- SOPs tab -->
    <li <%= (["sops"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %> >
      <%= link_to "SOPs", sops_url -%>
    </li>

    <% if current_user.is_admin? -%>
      <li class="tabs_block_gap">

      </li>
      <!-- JERM tab -->
      <li <%= (["jerm"].include?(controller.controller_name.to_s) ? ' id="selected_tabnav"' : '') %> class="tab_gap_before" >
        <%= link_to "JERM", jerm_url -%>
      </li>
    <% end -%>

  </ul>
  </div>
<% end  -%>
