<%= list_item_title resource -%>

<%= list_item_attribute "Start Date", resource.start_date.strftime('%d/%m/%Y') %>

<%= list_item_attribute "End Date", text_or_not_specified(resource.end_date.try(:strftime,'%d/%m/%Y')) %>

<%= list_item_attribute "Country", (country_text_or_not_specified resource.country) %>

<%= list_item_optional_attribute "City", h(resource.city) %>

<%= list_item_optional_attribute "#{t('event')} Website", h(resource.url), h(resource.url) %>

<%= list_item_description resource.description %>

<%= list_item_timestamp resource %>