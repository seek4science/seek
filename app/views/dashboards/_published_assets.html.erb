<canvas id="published-canvas"></canvas>

<% assets = @project.assets %>
<% published_count = assets.count { |a| a.is_published? } %>
<script>
    var config = {
        type: 'pie',
        data: {
            datasets: [{
                data: [
                    <%= published_count %>,
                    <%= assets.count - published_count %>
                ],
                backgroundColor: [
                    '#80ff64',
                    '#ff8a60'
                ],
                label: 'Dataset 1'
            }],
            labels: [
                'Published',
                'Not published'
            ]
        },
        options: {
            responsive: true
        }
    };
    var ctx = document.getElementById('published-canvas').getContext('2d');
    var myPie = new Chart(ctx, config);
</script>