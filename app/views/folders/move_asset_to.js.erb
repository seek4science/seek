$j('#<%= params[:asset_element_id] %>').fadeOut();

//FIXME: the rename_node doesn't seem to work, but all the docs and googling suggests it should
$j('#project_folders').jstree('rename_node', 'folder_<%=@dest_folder.id %>', '<%= @dest_folder.label %>');
$j('#project_folders').jstree('rename_node', 'folder_<%=@origin_folder.id %>', '<%= @origin_folder.label %>');

