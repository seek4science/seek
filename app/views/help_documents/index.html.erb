<% if admin_logged_in? -%>
    <%= button_link_to("Create New Page", 'new', new_help_document_path) -%>
<% end -%>

<h1>Help Index</h1>
<div style="padding-left: 2em;">
  List of topics:<br/>
  <ul class="bulleted_list">
    <%= "No topics found" if @help_documents.empty? %>
    <% @help_documents.each do |doc| %>
      <li><%= link_to doc.title, doc %></li>
    <% end %>
  </ul>
</div>