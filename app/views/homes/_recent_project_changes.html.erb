<% if logged_in? -%>
<% item_hash=recent_project_changes_hash -%>

<h2>New or recently changed items within your Projects</h2>

<div id="recent_project_changes_tabs">
  <%= render :partial => "assets/resource_listing_tabbed_by_class", :locals => { :resource_hash => item_hash,
    :narrow_view => true,
    :tabs_id=>"recent_changed_in_project_tabber",
    :authorization_already_done => true } -%>
</div>
<% end %>