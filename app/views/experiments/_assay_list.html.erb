<% selected ||= 0 %>
<%
  topics=[["Select an assay ..",0]]
  topics = topics | topic.assays.collect{|a| ["#{a.description}",a.id]} unless topic.nil?
%>

  <%=
    select_tag :experiment_assay_id,
               options_for_select(topics,selected),
               {:name=>"experiment[assay_id]",:disabled=>topic.nil?}
  %>

