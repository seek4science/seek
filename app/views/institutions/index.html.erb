<!--<%= link_to 'New institution', new_institution_path %>-->

<% if @institutions.public_methods.include?("page_count") %>
  <!-- only do the page links if the collection supports it -->
  <%= render :partial => "layouts/paginate", :locals => { :collection => @institutions } -%>
<% end %>

<%= render :partial=>"institution_list_item", :collection=>@institutions %>
