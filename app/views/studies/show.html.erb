<%
  @project =  @study.projects.first
%>

<% if Seek::Config.project_single_page_enabled && displaying_single_page? %>
  <%= render partial: 'general/single_page_treeview', locals: { item: @study,
                                                                content_partial: 'studies/show_main_content',
                                                                item_type: 'study', item_id: @study.id,
                                                                project: @study.projects.first} %>
<% else %>
  <%= render partial: 'show_main_content' %>
<% end %>