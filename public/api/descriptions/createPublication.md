<a name="createPublication"></a>A **createPublication** operation creates a new instance of a <a href="#publications">**Publication**</a>. The instance is populated with the content of the body of the API call.

The **createPublication** operation returns a JSON object representing the newly created <a href="#publications">**Publication**</a> and redirects to its URL.
